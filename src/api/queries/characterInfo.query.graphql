query characterInfo($id: Int!) {
  Character(id: $id) {
    name {
      full
      native
      alternative
      alternativeSpoiler
    }
    image {
      large
    }
    description
    gender
    dateOfBirth {
      year
      month
      day
    }
    age
    bloodType
  }
}
